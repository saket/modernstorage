name: Build

on:
    push:
        branches:
            - main
        paths-ignore:
            - '**.md'
    pull_request:

jobs:
    build:
        runs-on: ubuntu-latest
        timeout-minutes: 30

        steps:
            -   name: Checkout
                uses: actions/checkout@v2

            -   name: Copy CI gradle.properties
                run: mkdir -p ~/.gradle ; cp .github/ci-gradle.properties ~/.gradle/gradle.properties

            -   name: Set up JDK 17
                uses: actions/setup-java@v2
                with:
                    distribution: 'zulu'
                    java-version: 17
                    java-package: jdk

            -   name: Build root project
                run: ./gradlew clean && ./gradlew build

            -   name: Run local tests
                run: ./gradlew testDebug --stacktrace

#   TODO: Fix running instrumented tests on GitHub Actions
#    test:
#        needs: build
#        runs-on: ubuntu-latest
#        timeout-minutes: 30
#        strategy:
#            matrix:
#                api-level: [ 30 ]
##                api-level: [ 21, 23, 24, 26, 28, 29, 30, 31, 35 ]
#
#        steps:
#            -   name: Checkout
#                uses: actions/checkout@v2
#
#            -   name: Copy CI gradle.properties
#                run: mkdir -p ~/.gradle ; cp .github/ci-gradle.properties ~/.gradle/gradle.properties
#
#            -   name: Set up JDK 17
#                uses: actions/setup-java@v2
#                with:
#                    distribution: 'zulu'
#                    java-version: 17
#                    java-package: jdk
#
#            - name: Delete unnecessary tools 🔧
#              # From https://github.com/android/nowinandroid/blob/main/.github/workflows/Build.yaml
#              uses: jlumbroso/free-disk-space@v1.3.1
#              with:
#                  android: false # Don't remove Android tools
#                  tool-cache: true # Remove image tool cache - rm -rf "$AGENT_TOOLSDIRECTORY"
#                  dotnet: true # rm -rf /usr/share/dotnet
#                  haskell: true # rm -rf /opt/ghc...
#                  swap-storage: true # rm -f /mnt/swapfile (4GiB)
#                  docker-images: false # Takes 16s, enable if needed in the future
#                  large-packages: false # includes google-cloud-sdk and it's slow
#
#            - name: Enable KVM group perms
#              run: |
#                  echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
#                  sudo udevadm control --reload-rules
#                  sudo udevadm trigger --name-match=kvm
#                  ls /dev/kvm
#
#            - name: Setup Android SDK
#              uses: android-actions/setup-android@v3
#
#            - name: Accept licenses
#              run: yes | sdkmanager --licenses || true
#
#            - name: Run Android tests in GMD
#              run: ./gradlew pixel6Api${{ matrix.api-level }}Check
#                  --info
#                  -Dorg.gradle.workers.max=1
#                  -Pandroid.testoptions.manageddevices.emulator.gpu=swiftshader_indirect
#                  -Pandroid.experimental.testOptions.managedDevices.setupTimeoutMinutes=180
#                  -Pandroid.experimental.androidTest.numManagedDeviceShards=1
#                  -Pandroid.experimental.testOptions.managedDevices.maxConcurrentDevices=1
#                  -Pandroid.experimental.testOptions.managedDevices.emulator.showKernelLogging=false
